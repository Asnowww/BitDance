spring:
  datasource:
    url: jdbc:h2:mem:bitdance;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema:
  data:
    redis:
      host: localhost
      port: 6379

bitdance:
  jwt:
    secret: test-secret-must-be-at-least-32-bytes-long-1234567890
  sms:
    mock: true
    fixed-code: "123456"
