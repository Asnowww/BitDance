# BitDance 后端（Spring Boot 3 + Java 21）

待初始化。结构遵循 `BitDance_后端代码规范文档.md`，包根 `com.bitdance`，按 `iam / profile / catalog / review / practice / growth / content / workshop / merchant / settlement / governance / message / common` 业务域分包。

## 数据库

PostgreSQL 云库 `98.142.241.155:5432`，schema `bitdance`，凭证见 SKILL bitdance-dev 第 3 节，本地通过 `application-local.yml`（gitignore 范围内）注入，不得提交。

## 启动（待项目骨架就绪后填写）

- JDK：21
- 构建：Maven
- 端口：8080
- API 前缀：`/api`
