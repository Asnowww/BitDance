# 开发历史记录

## 项目概览

- 项目名称：BitDance
- 一句话定位：面向舞蹈学习者的找舞室、选课程、约搭子、记成长一站式平台
- 当前版本/里程碑：MVP（阶段 A 收尾、阶段 B 启动）
- 主要负责人：第五组（组长：顾远）

### 技术栈

- 用户/教练前端：Vue 3 + Vite 5 + TypeScript + Pinia + Vue Router (hash) + Vant 4，小红书视觉风格主色 `#FF2442`，移动端 H5（375 设计稿，最大宽度 480px 居中），后续以 Capacitor 打包 Android APK
- 管理端前端：Vue 3 + Vite + TypeScript（独立工程，桌面端），承载舞室管理员与平台管理员后台
- 后端：Spring Boot 3 + Java 21，模块化单体
- 数据库：PostgreSQL 云库（98.142.241.155:5432，schema bitdance）
- 缓存：Redis（待引入）
- 关键依赖：axios、postcss-px-to-viewport-8-plugin、unplugin-vue-components + VantResolver、Capacitor（后续阶段引入）

### 架构与目录

仓库根目录下：`frontend/` 用户/教练 H5 工程，`backend/` 后端工程（待初始化），`docs/history/` 历史归档目录，参考文档与 schema 位于根目录。详见 `BitDance_architecture_mvp.md`。

## 当前上下文快照

最后更新：2026-05-09

- 项目阶段：架构文档已完成从“微信小程序 + Vue Web 后台”到“Vue H5 移动端 + Vue Web 管理后台”的形态切换。前端工程脚手架初始化完成，处于阶段 A 与阶段 B 切换点。
- 当前焦点：按循环开发模式推进 H5 前端的阶段 B 主闭环（M3 登录/资料、M1 舞室/课程/试听、M2 评价、M4 约练、M5 打卡/成长时间线），后端尚未启动，全部走 mock adapter。
- 最近成功运行状态：脚手架级别的页面骨架与 mock 数据接通；首页瀑布流可基于 mock studio 数据渲染。`npm install` 与 `npm run dev` 由用户在本地执行验证。
- 未完成工作：登录/验证码、舞室列表筛选、舞室详情、课程详情、教练详情、收藏、试听预约、评价主流程、约练发布与广场、训练打卡、成长时间线、用户中心与隐私设置、底部 TabBar 中央凸起发布按钮、PullRefresh / 无限加载、首页搜索与筛选浮层、收藏聚合页、消息中心、APK 打包配置等。

## 约定与规范

见 SKILL bitdance-dev 第 7 节与 `BitDance_后端代码规范文档.md`。前端额外约束：

- 路由强制 hash 模式，禁止切换为 history 模式，否则后续 Capacitor 打包 APK 会失效。
- 视觉风格统一参照小红书：主色 `#FF2442`、瀑布流首页、沉浸式详情页、底部 TabBar 中央凸起发布按钮。
- 接口统一通过 `src/utils/request.ts` 调用，业务接口拆分到 `src/api/<domain>.ts`，mock 数据集中在 `src/mock/modules/<domain>.ts`。
- 组件文件 PascalCase.vue；store / composable / 工具方法 camelCase；样式以 SCSS 模块化，主题变量集中在 `src/styles/variables.scss`。

## 禁区与陷阱

- 不要修改 `bitdance_postgresql_schema.sql` 中已使用表的字段类型，只允许新增字段或新增表。
- 前端路由必须保持 hash 模式，否则后续 APK 打包会失效。
- `VITE_USE_MOCK` 切换前必须确认所有接口在真后端已实现，否则页面会大面积报错。
- 云数据库凭证不得 push 到公开仓库；本地配置走 `application-local.yml`，并已在根 `.gitignore` 内。
- `.claude/` 目录已在根 `.gitignore` 内忽略，提交前 `git status` / `git diff --cached` 必须自查一遍，禁止把 worktree、settings.local.json、skill 内容入库。
- 提交署名只写人类成员，禁止出现 ChatGPT / Claude / Anthropic / OpenAI / Copilot / Cursor / AI / LLM / agent 等任何 AI 工具或机器协作者标识。

## 环境与依赖

### 本地搭建

前端：

```bash
cd frontend
npm install
npm run dev
```

默认 `VITE_USE_MOCK=true`，无需后端即可联通。

后端：待初始化，初版骨架交付后补全 README。

### 环境变量

| 变量名 | 用途 | 示例值 |
| --- | --- | --- |
| VITE_API_BASE | 前端真后端地址 | http://localhost:8080/api |
| VITE_USE_MOCK | 前端是否启用本地 mock | true / false |
| SPRING_DATASOURCE_URL | 后端数据库连接 | jdbc:postgresql://98.142.241.155:5432/bitdance?currentSchema=bitdance |
| SPRING_DATASOURCE_USERNAME | 数据库用户 | BitDance |
| SPRING_DATASOURCE_PASSWORD | 数据库密码 | （占位，见 SKILL 第 3 节） |

### 数据初始化

执行 `bitdance_postgresql_schema.sql` 即可。

### 构建与部署

H5：`npm run build` 输出到 `frontend/dist/`，部署为静态站点；APK 阶段使用 Capacitor。
后端：待补。

## 接口与数据契约

### 主要 API

由 Architect Agent 在每个功能点开发前补全，记录在该区块。当前已约定：

- `POST /auth/sms/send`：手机号短信验证码发送，请求体 `{ phone }`，响应 `{ sent, expiresIn }`。
- `POST /auth/login`：手机号 + 验证码登录，请求体 `{ phone, code }`，响应 `{ token, user }`，其中 `user = { id, phone, nickname, avatar, roles }`。
- `GET /studios/nearby`：附近舞室列表，参数 `latitude / longitude / city / page / pageSize / sort`，响应 `{ list, page, pageSize, total }`，列表项含 `id / name / cover / city / area / distanceKm / ratingAvg / reviewCount / topStyles / beginnerFriendly`。
- `GET /studios/{id}`：舞室详情，附 `intro / address / openHours / photos / courses`。

### 数据库表结构

以 `bitdance_postgresql_schema.sql` 为准。

### 模块间通信

前后端通过 RESTful + Bearer token，统一返回 `{ code, message, data, traceId }`，前端拦截 `code === 0 / 200 / 'SUCCESS'` 视为成功。

## 开发日志

### 2026-05-09 ｜ 第五组

- 目标与背景：评审后决定将用户端与教练端从微信小程序统一到 Vue 3 H5 移动端，先以浏览器 H5 上线、后续以 Capacitor 打包为 Android APK；同时启动循环开发模式补全前端骨架，避免双轨开发拉长 MVP 周期。
- 改动范围：
  - `BitDance_architecture_mvp.md` 顶部新增修订记录，并在 1.1、2.1、2.2、2.6、2.7、4.1-4.7、7.1-7.7 节将“用户/教练微信小程序”统一替换为“用户/教练 H5 端”/“Vue H5 移动端”，新增 `/api/h5/**` 路径前缀并保留 `/api/mp/**` 双写过渡说明，调整 `WechatAuthService` 描述为 MVP 阶段以手机号验证码登录为主。
  - 新建 `frontend/` 工程：`package.json`、`vite.config.ts`、`postcss.config.cjs`、`tsconfig.json`、`index.html`、`.env.development`、`.env.production`、`.gitignore`、`README.md`、`src/main.ts`、`src/App.vue`、`src/router/index.ts`、`src/utils/request.ts`、`src/mock/index.ts` 与 auth/studio 模块、`src/styles/index.scss` 与 `variables.scss`、`src/pages/{home,common,practice,publish,growth,user}` 占位页面、`src/types/{env,components}.d.ts`、`src/shims-vue.d.ts`。
  - 新建 `backend/README.md` 占位、`docs/history/.gitkeep`、`开发历史记录.md`（本文件）。
- 关键问题与解决：架构文档大量散落 `小程序`、`用户/教练小程序`、`教练小程序` 等表述，采用 `replace_all` 分批替换并显式排除“不再开发微信小程序”这类有意保留的修订说明文本，确保语义不被误覆盖。
- 方案选择理由：单一前端工程比并行维护 H5 + 微信小程序节省至少一半工程量，且 Capacitor 打包路径相对成熟；hash 路由可同时兼容浏览器、APK WebView 与本地预览；mock adapter 拦截 axios 适配器以零依赖方式联通，避免引入 mockjs 等额外运行时。
- 待办与遗留：登录页、舞室筛选浮层、详情页、评价主流程、约练流程、打卡与成长时间线、底部 TabBar、APK 打包配置；后端工程尚未初始化；npm install 在用户本机执行后再做联通自测。
- 关联：commit `f7201b8`，远端 https://github.com/Asnowww/BitDance/commit/f7201b8 ，分支 `origin/master`。

### 2026-05-10 ｜ 第五组 ｜ FP-001 应用骨架与底部 TabBar

- 目标与背景：循环开发首个功能点，搭建 H5 主导航骨架，确立小红书风格的中央凸起发布按钮交互范式，为后续功能页统一注入 Tab 高亮与 ActionSheet 发布入口。
- 改动范围：新增 `frontend/src/components/AppLayout.vue` 与 `AppTabBar.vue`；`App.vue` 改为渲染 `AppLayout`；新增 `pages/publish/PublishCheckinPage.vue`、`PublishPracticePage.vue`、`PublishReviewPage.vue` 占位；删除原 `PublishEntryPage.vue`；`router/index.ts` 把 `/publish` 拆成 `/publish/checkin /publish/practice /publish/review` 三条子路由，去掉发布按钮的 tab meta。
- 关键问题与解决：发布按钮要中央凸起，常规 `<van-tabbar>` 难以原生表达 5 列且中间凸起的形态，改为自实现 `grid-template-columns: repeat(5, 1fr)` + 中央按钮 `transform: translateY(-14px)`；底部安全区通过 `env(safe-area-inset-bottom)` 同时影响 tabbar 高度与主内容 `padding-bottom`，避免被遮挡。
- 方案选择理由：自实现 TabBar 可以精确控制中央凸起的偏移、阴影与圆角，且后续替换为 SVG 图标只需替换 emoji 占位；发布按钮使用 `showActionSheet` 而不是另起一个抽屉页面，降低交互层级，符合“一键发布、下钻选择”的小红书范式。
- 待办与遗留：emoji 图标占位需要替换为 Vant 内置图标或自有 SVG；未登录拦截依赖 FP-003；publish 子页内容由 FP-012/015/018 实现；ActionSheet 无障碍焦点管理待 FP-024 一并梳理。
- 关联：commit 待 push。

## 决策记录

### 2026-05-09：用户端与教练端统一为 Vue H5 移动端，不再开发微信小程序

- 决策内容：用户端与教练端共用同一份 Vue 3 + Vite + TS + Vant H5 工程，先以浏览器 H5 上线，阶段 B 收尾后使用 Capacitor 打包为 Android APK 分发。
- 上下文与权衡：原方案双轨开发微信小程序与 Web 管理端，资源不足；统一为 H5 后失去微信生态原生入口与订阅消息能力，但获得跨平台一致性与更短交付周期，符合实训项目优先验证“是否有人在平台上做决策”的目标。
- 影响范围：架构文档（已修订）、API 路径（新增 `/api/h5/**`，保留 `/api/mp/**` 双写过渡）、登录方式（MVP 阶段以手机号验证码为主，微信登录降级为可选项）、消息通知（订阅消息降级为站内信 + 浏览器通知 + 后续 APK 推送）。
- 回滚条件：若实测中 H5 体验在低端 Android 设备或浏览器内核上无法满足核心交互，且 Capacitor 打包 APK 仍不能解决，则评估退回到微信小程序或独立 React Native / Flutter 方案。

## 变更日志

### v0.1.0 ｜ 2026-05-09

- 架构文档完成 Vue H5 形态切换；前端 Vue 3 + Vite + TS + Vant + 小红书风格基础脚手架就绪；首页瀑布流跑通 mock 数据；后端工程占位文档就绪。
